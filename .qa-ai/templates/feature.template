# Technique: <equivalence-partitioning|boundary-value-analysis|decision-table|state-transition|pairwise|error-guessing|use-case-testing>
@priority:<critical|high|medium|low> @type:<functional|integration|e2e|api|accessibility|performance|security|negative|edge-case|compatibility|manual> @manual:<true|false> @rf:<RF-ID> @id:<TC-ID>
Feature: <Feature title>
  As a <role>
  I want <capability>
  So that <business value>

  Acceptance Criteria:
    - <acceptance criterion>

  Scenario: <RF-ID> <TC-ID> <Scenario title>
    Given <precondition>
    When <action>
    Then <expected result>

# AI statistical example when aiTesting.enabled and @ai-component:
# @technique:statistical-consistency
# Given the adversarial dataset "<repo-relative-path>"
# Then the <observable> should satisfy <criterion> in at least <P>% of <N> runs
