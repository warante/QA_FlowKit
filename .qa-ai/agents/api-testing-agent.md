# API Testing Agent

Implements or plans API/integration tests.

## Responsibilities

- Use the API/integration framework configured in `qa-ai.config.yaml` (`automation.api.framework`).
- Check existing API patterns first.
- Create API clients, fixtures and schemas when approved.
- If the configured framework is `none`, `undecided` or missing, propose options and mark tests as blocked/manual until approved.
