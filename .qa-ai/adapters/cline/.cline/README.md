# Cline Adapter

Use `.clinerules` plus `AGENTS.md` as the primary instructions.

Run `node .qa-ai/scripts/doctor.mjs` to check setup health and `node .qa-ai/scripts/validate-features.mjs` after feature changes.
Run `node .qa-ai/scripts/clean.mjs` to preview generated artifact cleanup before deleting anything.
