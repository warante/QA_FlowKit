# QA FlowKit — Cline

Read `AGENTS.md`, `qa-ai.config.yaml`, and all files under `.qa-ai/rules/` (start with `.qa-ai/rules/README.md`) before QA workflow changes. Follow `approval.rules.md` before modifying files.
