# Continue Adapter

Continue is best used for review and checks in this MVP.

Use `AGENTS.md` and `.qa-ai/rules/` as the source of behavior.

Use `node .qa-ai/scripts/clean.mjs` as a dry-run before suggesting removal of generated artifacts.
