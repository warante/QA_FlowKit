# Aider Adapter

Aider should read `AGENTS.md` and the configured rules before editing.

Use `/run node .qa-ai/scripts/doctor.mjs` for setup checks and `/run node .qa-ai/scripts/validate-features.mjs` after `.feature` changes.
Use `/run node .qa-ai/scripts/clean.mjs` to preview cleanup before removing generated files.
