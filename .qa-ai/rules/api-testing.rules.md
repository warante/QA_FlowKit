# API Testing Rules

- Default API/integration framework: Playwright API when configured.
- Check existing API testing patterns before creating new ones.
- Use clients, fixtures and schemas for maintainability.
- Do not add dependencies without approval.
- If API framework is undecided, create a proposal and keep tests as manual/blocked until decided.
